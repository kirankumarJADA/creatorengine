package com.creatorengine.autopilot.entity;

/** Preset persona templates for AI Autopilot (#15). CUSTOM uses only the creator's own system prompt. */
public enum AutopilotRole {
    SALES_ASSISTANT,
    CUSTOMER_SUPPORT,
    COACH,
    CUSTOM
}
